export const GPU_BRACKET_SPEC = Object.freeze({
  thickness: 2, // Bracket thickness along X
  height: 120, // Bracket height along Y
  width: 38.5, // Bracket width along Z (slot coverage)
  dropBelowBody: 30, // How far the bracket bottom sits below the GPU body bottom
  xOffset: -0.8, // Offset from the body's -X face so the bracket sits just outside
});
